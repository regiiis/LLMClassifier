# Configuration file for linting. Defines rules for the pre-commit hook yamllint in .pre-commit-config.yaml

extends: default

ignore:
  - .github/workflows/main_pipeline.yml

rules:
  line-length:
    max: 120
    level: warning
  document-start:
    present: false
